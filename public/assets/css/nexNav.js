import React from 'react';
import {StyleSheet} from 'react-native';
const nextNavCss = StyleSheet.create({
  view: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-between',
    width: '50%',
    marginLeft: 'auto',
    marginRight: 'auto',
    marginBottom: 50,
  },
});

export default nextNavCss;
